<?php

/**
 * Table representing the framework's security rules
 * Tableau représentant les règles de sécurité du framework
 */
return [

    /*
     * Php session lifetime
     * Durée de vie des sessions php
     */
    'sessionLifetime'  => 7200,

    /*
     * If True, the framework will check the permissions (notably the banned IP)
     * Si True, le framework vérifiera les permissions (Notamment les IP bannies)
     */
    'checkPermissions' => false

];